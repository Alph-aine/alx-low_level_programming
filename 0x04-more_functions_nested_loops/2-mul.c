#include "main.h"
/**
 * mul-entry point of the function
 *@a: first number
 *@b: scond number
 *
 * Description: function takes in 2 numbers and multiply them
 * Return: return c
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
