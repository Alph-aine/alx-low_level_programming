#include <stdio.h>
/**
 *main- entry point into the  program
 *@argc: the number of arguments passed while executing the program
 *@argv:the valus of those arguments
 *Return: 0
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
